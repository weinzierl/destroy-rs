[package]
name = "bevy_serde_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
serde = { version = "1", features = ["derive"] }
ron = "0.10"

# Recommended for faster dev builds with Bevy
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
