[package]
name = "bevy_sprite_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"

# Enable optimizations for dependencies in dev builds (huge speedup for Bevy)
[profile.dev.package."*"]
opt-level = 3
